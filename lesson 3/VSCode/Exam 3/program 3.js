//a function to find the last value of an array
function getLast(list) {
    //find the last element of the list and its value.
    let last = list[list.length - 1];
    //return the value of the last element
    return last;

}