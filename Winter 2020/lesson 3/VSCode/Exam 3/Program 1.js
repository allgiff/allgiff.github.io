//a function to return a name in this format = "family name, given name"
function makeFullName(givenName, familyName) {
    //format the two names together, seperated with a comma
    let fullname = familyName + ", " + givenName;
    //return the full name
    return fullname;

}